# --- DATABASE CONFIG ---
# WARNING: Always change these for non-local deployments.
POSTGRES_USER=coclerk
POSTGRES_PASSWORD=coclerk_password
POSTGRES_DB=coclerk_dev

# Combined database URL for application use
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@localhost:5432/${POSTGRES_DB}
API_PORT=4000
NODE_ENV=development
JWT_SECRET=super_secret_local_dev_key
